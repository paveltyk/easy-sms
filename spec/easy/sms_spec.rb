require 'spec_helper'

describe Easy::Sms do
  it 'has a version number' do
    expect(Easy::Sms::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
